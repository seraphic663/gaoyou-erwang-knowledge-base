# 项目一致性检查清单

本文件用于提醒：改网站、数据库、申报资料或 README 时，必须同步检查相关文件。当前目录结构以 `01-项目资料 / 02-数据库 / 03-项目网站 / 04-项目文献 / 05-组会谈话` 为准。

## 一、核心口径

- 项目名称：高邮二王考据过程知识库构建及应用。
- 展示定位：研究整理原型，不写成泛化古籍平台。
- 数据库定位：一个专题数据库，多种浏览视角。
- 数据链路：`main/source.txt -> main/parser.py -> main/importer.py -> data/dictionary.db -> sqlite_bridge.py -> sqlite-snapshot.json`。
- 标注链路：`DOCX -> 04-项目文献/D-标注/json/run.py -> data/annotations.db -> annotation_bridge.py -> annotation-snapshot.json`。
- 代表案例：首页当前以《经义述闻·左传下》“造舟于河”为主线案例。

## 二、改动联动规则

| 如果修改 | 必须同步检查 |
| --- | --- |
| `03-项目网站/index.html`、`app.js` | 根 `README.md`、网站 `README.md`、网站更新记录、申报资料中项目名称和展示口径 |
| `database.html`、`browser.js`、`term.html`、`case.html`、`detail.js` | 数据库 README、网站 README、`src/store-definitions.js`、数据统计是否一致 |
| `annotation.html`、`annotation.js` | 标注库 README、网站 README、`annotation-snapshot.json` |
| `ai-annotation.html`、`ai-annotation.js`、`src/ai-annotation.js` | 网站 README、DeepSeek API key 说明、AI 引用展开交互 |
| `02-数据库/main/`（source.txt、parser.py、importer.py、database.py） | `02-数据库/README.md`、`03-项目网站/scripts/sqlite_bridge.py`、网站快照 |
| `02-数据库/annotation/` 或标注库 schema 变更 | `02-数据库/README.md`、`03-项目网站/scripts/annotation_bridge.py`、annotation-snapshot.json |
| `03-项目网站/data/sqlite-snapshot.json` | 首页统计、数据库页统计、申报材料中的数据量表述 |
| `03-项目网站/src/http-server.js` API 变更 | `03-项目网站/README.md`、部署迁移说明、健康检查和演示检查清单 |
| `Dockerfile`、`railway.toml`、根目录 `package.json` | 根 `README.md`、网站部署报告、迁移说明、线上 `/api/health` |
| `04-项目文献/0-当前阅读/` | 首页案例、知识页术语、文献 README |
| 根 `README.md`、各目录 `README.md` | 网站更新记录、冗余性分析报告 |

## 三、提交前检查

每次提交前至少做这些检查：

1. 搜索旧路径：`01-项目申报`、`01-立项申报`、`D-二级资料`、`F-标注`。
2. 搜索旧数据源：`demo-db.json`、`parsed_data.py`、`dictionary.empty.db`、`step2.png`。
3. 搜索旧口径：`两套数据库`、`字词数据库`、`案例数据库`。
4. 搜索旧部署片段：`COPY server.js ./`、`NIXPACKS`。
5. 确认运行时文件不入仓：`.env`、`__pycache__/`、`*.db-wal`、`*.db-shm`。
6. 如果改了前端脚本，运行 `node --check`。
7. 如果改了主库脚本，运行 `python 02-数据库/main/importer.py --dry-run`。
8. 如果改了 SQLite 主库，运行 `npm run sync:sqlite`。
9. 如果改了 SQLite 标注库，运行 `npm run sync:annotation`。
10. 提交前运行 `git diff --check`。

## 四、当前保留边界

- `02-数据库/main/parser.py`、`main/importer.py`、`main/database.py` 必须保留，因为它们解释数据库如何重建。
- `02-数据库/lib/` 是共享工具层，被主库、标注库和桥接脚本共用。
- `02-数据库/data/` 放两个 SQLite 产物：`dictionary.db` 和 `annotations.db`。
- `parsed_data.py` 不再保留，因为它是生成型中间文件。
- Python 脚本重新计入 GitHub 语言统计；当前体量不会造成项目语言比例畸形。
- `03-项目网站/data/sqlite-snapshot.json` 是网站展示快照，不要手工改，优先从 SQLite 导出。
- `03-项目网站/data/annotation-snapshot.json` 是人工标注灰度库快照，不要手工改，优先从标注库导出。
- 首页不保留累计访问计数；线上容器不应依赖运行时 JSON 做持久化展示。
