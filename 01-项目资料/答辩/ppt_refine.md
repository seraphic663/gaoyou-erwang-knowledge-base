# PPT 修改清单

只保留尚未落实到当前 `slide.tex` 的修改项。已经改好的内容不再列入。

## P4 研究对象

- 在本页右侧或下方加入两张人物图：`王念孙.jpg`、`王引之.jpg`。
- 图片下方分别标注：`王念孙（1744-1832）`、`王引之（1766-1834）`。
- 将现有项目符号压缩为 3 条以内，避免人物图加入后版面拥挤。
- 目的：让非专业评委先知道“高邮二王是谁”。

## P8 考据结构流程图：据王宁《训诂学原理》

- 将页面中的流程明确改为六环节：`发疑 -> 预设 -> 取证 -> 释理 -> 结论 -> 类推`。
- 如果继续使用 `step.png`，则在图下方补一句：`六环节对应：发疑、预设、取证、释理、结论、类推。`
- 更好的做法：用 TikZ 直接画六个流程框，替换当前图片。
- 目的：避免现在图注只有“发疑—设问—取证—释理—结论”，缺少“预设、类推”。

## P9 训诂学内容怎样进入系统

- 将表格从平行罗列改成层级关系。
- 建议表格改为：
  - `文意训释 / 词义训释`：区分句意判断与字词训释。
  - `形训、声训、义训`：作为词义训释下的具体方法。
  - `形音义互求`：作为证据互证的基本原则。
  - `望文生训`：作为旧说裁断与风险提示。
  - `因声求义`：作为处理声训、通假问题的路径。
- 目的：让评委看到术语之间有层级，不是堆概念。

## P10 代表案例：“造舟于河”

- 可在本页之后新增一页网站截图。
- 截图内容优先选“造舟于河”案例详情页或数据库检索页。
- 页面标题可用：`网站呈现：“造舟于河”案例`。
- 目的：前一页讲文本逻辑，后一页展示同一案例如何进入系统。

## P18 泛化能力

- 保留当前标题“泛化能力”。
- 将当前两栏列表改成两个方向：
  - `材料泛化`：可迁移到《诗经》《尚书》、经传注疏争议条目、出土文献材料。
  - `平台推广`：可迁移到清代考据笔记、小学专书、课堂阅读训练平台。
- 底部保留：`案例粒度`、`证据类型`、`过程标注`、`人工复核`。
- 目的：把“能迁移到哪里”讲得更明确。

## 重点问答

1. 人机互动真实吗？准备一个 AI 出错、人工修正、再迭代的案例。
2. AI 的位置是什么？回答为：相似案例召回、证据片段匹配、结构草案生成、复核点提示。
3. 技术路线为什么适合？准备比较：大模型、数据库、规则系统、知识图谱。
4. 学术依据是什么？准备王宁《训诂学原理》、高邮二王研究、清代考据学研究、数字人文平台案例。
5. 数据口径是什么？主库是稳定展示，标注库是工作稿和 AI 整理材料，二者不要混讲。
